# Windows Native Adapter

Target shape: shared desktop shell wrapped with Windows capabilities.

First native capabilities to implement:

- system tray app
- global prompt shortcut
- screenshot capture
- UI Automation API adapter
- filesystem and Office document workflows
- browser automation adapter
- local model runner integration

Windows should share most of the desktop shell with macOS and Linux while keeping automation and permission code behind a platform adapter boundary.

